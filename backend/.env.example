PORT=4000

DB_HOST=localhost
DB_PORT=3306
DB_NAME=jobniquefinal
DB_USER=root
DB_PASSWORD=YourPasswordHere

JWT_SECRET_KEY=qweasd
JWT_EXPIRE=7d
COOKIE_EXPIRE=7

FRONTEND_URL=http://localhost:5173

# Resumes are stored locally on disk (backend/uploads/resumes) — no
# Cloudinary/S3 keys needed for that. Left here only if you later want
# to swap to cloud storage.
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Required for AI features (chat assistant, resume analysis, job recommendations)
# Get a free key at https://console.groq.com
GROQ_API_KEY=YourGroqApiKeyHere
GROQ_MODEL=llama-3.1-8b-instant
